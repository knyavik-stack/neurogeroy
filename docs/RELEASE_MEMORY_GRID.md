## 2026-09-03 — Memory Grid exposed

- Memory Grid is now exposed from the existing home screen through a DOM-level integration in the Worker entry layer.
- The legacy Lightning source file remains untouched.
- The Memory Grid interaction was corrected to match its visual design: highlighted cells form a static pattern, so the player reproduces the set of cells rather than an unseen sequence order.
- Production route: `/games/memory-grid`.
- Home navigation: `🧠 Память-сетка`.
